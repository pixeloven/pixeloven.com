/** @type {import('tailwindcss').Config} */
const tailwindConfig = {
    content: [
        "./**/*.{ts,tsx}",
    ],
    theme: {
        extend: {},
    },
    plugins: [],
};

module.exports = tailwindConfig;